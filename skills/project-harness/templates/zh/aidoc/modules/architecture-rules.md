<!-- last-updated: {{DATE}} -->
# 架构与模块组织规则（architecture rules）

> 本项目架构必须遵守的约束。所有规则必须引用实际代码中的类名和文件路径；TODO 项由 generate 工作流按真实代码填充。

## 总原则

- 严格分层，禁止跨层调用、禁止反向依赖
- 每层只通过下层的公开接口交互
- TODO: 本项目的实际分层调用链（如 Endpoint → Service → Model）

<!-- 变体：分层服务 / 包模块结构 / 插件体系，generate 工作流保留匹配变体并删除其余；下方各层小节按实际范式调整 -->

<!-- 变体说明：
- 分层服务（典型 Web/后端）：按 数据层 → 契约层 → 业务逻辑层 → 入口层 组织
- 包模块结构（library/SDK）：按公开 API 面 → 内部实现模块 → 共享基础设施 组织
- 插件体系（cli/可扩展工具）：按核心内核 → 插件契约/接口 → 插件实现 → 注册机制 组织
- qt-app：UI 与逻辑分层，信号槽连接两侧；UI 层不直接操作核心数据结构
- cpp-app：按 公开头文件/接口 → 实现模块 → 构建目标（CMake/Makefile）组织
- java-app：按 package 分层（如 controller/service/repository 或 api/impl），依赖方向单向
-->

## 数据层

<!-- TODO: 按真实代码填写；本节标题为通用表述，按实际范式调整（如 Model 层 / 存储层 / 内核数据模块） -->

- 基类继承：TODO:（引用实际基类，如 `app/models/base.py:Base`）
- 字段声明方式：TODO:
- 表名/集合命名规则：TODO:
- 存放位置：TODO:

## 契约层

<!-- TODO: 按真实代码填写；本节标题为通用表述，按实际范式调整（如 Schema/DTO 层 / 公开类型与接口层 / 插件接口层） -->

- 请求/响应基类：TODO:
- 序列化规则：TODO:
- 验证方式：TODO:
- 存放位置：TODO:

## 业务逻辑层

<!-- TODO: 按真实代码填写；本节标题为通用表述，按实际范式调整（如 Service 层 / 核心逻辑模块 / 命令处理器） -->

- 只放纯业务逻辑，禁止处理传输层/入口层细节
- 方法签名模式：TODO:
- 异常处理：TODO:（引用实际异常类）
- 查询优化：TODO:（如分页、N+1 规避约定）

## 入口层

<!-- TODO: 按真实代码填写；本节标题为通用表述，按实际范式调整（如 Controller/Endpoint 层 / 公开导出模块 / CLI 命令入口） -->

- 只负责参数提取、调用业务逻辑层、输出格式化
- 分页处理：TODO:
- 响应格式：必须遵循 [../contracts/boundary.md](../contracts/boundary.md) 的统一契约结构

## 注册机制

<!-- TODO: 路由/命令/插件的注册方式与位置；无注册机制则删除本节 -->

TODO:

## 错误码分配

<!-- TODO: 如有错误码体系，列出已使用的错误码范围；无则删除本节 -->

| 范围 | 用途 |
|---|---|
| TODO: | TODO: |
