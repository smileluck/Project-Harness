<!-- last-updated: {{DATE}} -->
# 前后端边界与数据契约（boundary）

> 前后端责任边界与数据契约的唯一维护位置。契约结构必须来自真实代码；backend-only 项目将本文改为 API 接口契约，frontend-only 项目改为 API 消费契约。

## 责任边界

<!-- TODO: 按实际职责划分填写 -->

| 职责 | 归属 |
|---|---|
| TODO: 如 数据校验/业务规则/鉴权 | 后端 |
| TODO: 如 表单即时反馈/展示格式化 | 前端 |

## 统一响应结构

<!-- TODO: 根据实际代码填写 JSON 结构和字段说明，并引用定义该结构的代码位置 -->

```json
TODO: 真实响应结构示例
```

| 字段 | 类型 | 说明 |
|---|---|---|
| TODO: | TODO: | TODO: |

定义位置：TODO:（如 `app/schemas/response.py:ResponseModel`）

## 统一分页结构

<!-- TODO: 如有分页约定，填写字段说明与代码位置；无则删除本节 -->

| 字段 | 类型 | 说明 |
|---|---|---|
| TODO: | TODO: | TODO: |

## 字段命名规范

<!-- TODO: snake_case / camelCase 的实际约定，含两侧是否做转换、转换发生在哪一层 -->

TODO:

## 关键类型桥接

<!-- TODO: 如有特殊类型转换（如 bool ↔ string、Decimal ↔ number、枚举 ↔ 文案），必须详细说明转换流程和涉及的代码位置；无则写"无" -->

TODO:

## 时间字段

<!-- TODO: 格式与时区约定（如 ISO 8601 / UTC），含序列化位置；无特殊处理则删除本节 -->

TODO:

## 变更规则

1. 契约字段的新增/改名/删除属于接口改动，必须同步契约两侧（生产方与消费方）
2. 契约变更必须跑 typecheck + 契约级测试（见根 `AGENTS.md` 操作不变量）
3. 破坏性变更必须先写决策记录（见 [../notes/README.md](../notes/README.md)），并说明迁移与回滚方式

## 完成前检查清单

- [ ] 两侧字段名、类型、可空性一致
- [ ] 类型桥接在约定位置完成，未散落各处
- [ ] 错误响应对前端可预期（错误码/消息结构一致）
- [ ] 相关测试已覆盖契约变更点
