<!-- last-updated: {{DATE}} -->
<!-- lesson-meta: status=pending count=1 post=0 target= -->
# Lesson Record Template

> Path: `aiDoc/memory/lessons/yyyy-mm-dd-topic.md`. Keep it light: filling in this file completes the capture.
> The header `lesson-meta` marker is the sole basis for mechanical scanning: `status` (pending/deferred/promoted/dropped), `count` (occurrences before promotion), `post` (recurrences after promotion), `target` (when promoted: repo-relative path of the rule's home file). Whenever the count/status below changes, **sync this marker**.

## Context

<!-- What task / which code location triggered it; one sentence -->

## Pitfall / Pattern

<!-- The concrete pitfall or the reusable pattern observed; why it is likely to recur -->

## Occurrences

<!-- 1 when first filed; on each recurrence increment in place and append one line of new context; sync `count` in lesson-meta -->

## Status

<!-- pending / deferred / promoted / dropped; sync `status` in lesson-meta. pending with count ≥ 2 is blocked by the mechanical check: either promote, or set deferred and record the defer reason under "Promotion Target" -->

## Promotion Target

<!-- When promoted: which file and section the rule was written into (relative path + section name), and sync `target` in lesson-meta; when deferred: the defer reason; when dropped: the reason -->

## Recurrence After Promotion

<!-- Keep 0 while there is no recurrence. If the same problem recurs after promotion: increment `post` in lesson-meta, revise the promoted rule body in the same change (do not file a new lesson), and record per aiDoc/notes/README.md why the first rule failed to prevent it -->

## Recorded

<!-- YYYY-MM-DD; update to the latest occurrence date when incrementing -->
